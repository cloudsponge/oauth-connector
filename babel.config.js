// this file exposes the babel configuration which is shared by
// jest and rollup
module.exports = {
  presets: [
    [
      '@babel/preset-env',
      // {
      //   debug: true,
      // },
    ],
  ],
}
